define hello(testInt: integer)
variables test : integer
constants yello = ~0.31
    test[test[49 + 4]] := test[4] / 8 + random
    random := 40.2
    bool := test[90] and 90 or 90 not 90 <> (90 <= 90 >= 90) = 90 < (90) > 90

define goodbye()
variables            yo: real
constants forty, fortyOne=   40
    bool := 90 and 80