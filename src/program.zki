define start()
variables thought: boolean
variables shouldBeOneHundred: integer
    testForInitializeFailure 5

define testForInitializeFailure(incomingInt: integer)
    if incomingInt = 0
        write "Total and complete failure!"
    else
        write "Incomplete failure!"
    write " in testForInitializeFailure: ", incomingInt